/**
 * @jsx React.DOM
 */
var React = require('react');

var Contact = React.createClass({
  render: function() {
    return (
      <div>
        contact
      </div>
    );
  }
})

module.exports = Contact;